# local data sources (symlinks into sibling projects)
/catalog
/data

# pipeline intermediates (SkyServer pulls, merged catalogs, fetched cutouts, quicklooks)
pipeline/cache/
__pycache__/
.venv/
*.pyc

# dev-only synthetic dataset and test output
site/data-synth/
site/tests/screens/
node_modules/
.DS_Store

# old Artifact build output (the build script is retired; the site is live on Pages)
dist/
